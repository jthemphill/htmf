[package]
name = "selfplay"
version = "0.1.0"
authors = ["Jeff Hemphill <jhemphill@fb.com>"]

[dependencies]
rand = "0.7.2"
rayon = "1.2.1"

htmf-bots = { path = "../bots" }
htmf = { path = "../engine" }
