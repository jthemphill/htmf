[package]
name = "selfplay"
version = "0.1.0"
authors = ["Jeff Hemphill <jhemphill@fb.com>"]

[profile.release]
debug = true

[dependencies]
rand = "0.5"
rayon = "1.0.2"

htmf-bots = { path = "../bots" }
htmf = { path = "../engine" }
