[package]
name = "selfplay"
version = "0.1.0"
authors = ["Jeff Hemphill <jhemphill@fb.com>"]
edition = "2021"

[dependencies]
rand = "0"
rayon = "1"

htmf-bots = { path = "../bots" }
htmf = { path = "../engine" }
