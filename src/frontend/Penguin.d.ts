import * as React from "react";
import "./index.css";
interface Props {
    size: number;
    player: number;
}
export default function Penguin({ size, player }: Props): React.JSX.Element;
export {};
